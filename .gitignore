# Full local pipeline output (~780MB) - never push this.
# deploy_artifacts/ is deliberately NOT listed here - it's the small
# (~20MB) subset ./deploy.sh copies out of these, and is meant to be
# committed so a git-based deploy host has what it needs after a pull.
data/raw/*.csv
data/processed/*.csv
models/
results/
catboost_info/

*.zip
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/
venv/
docs/
PLAN.md
UNDERSTANDING.md
.claude/
